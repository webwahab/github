##Github 

# Create repository
# install git using this command line in Ubuntu:
sudo apt-get git
# To clone a poject you should fist copy the http link of githup repository. After that you should use the commande clone 
For example: git clone https://github.com/webwahab/github.git
#Setting the global variable of the repository
For the user name: git config user.name "Abdelwahab"
For the Email: git config user.email "webwahab@gmail.com"
To display one of the two for example:
git config user.name
# To get status of the repository: you should type the following command 
git status
# To add a file to repository:
git add name_file
#To commit the change on the repository we should use commit 
git commit -m "the performed change"
#To show all the commit we use: git log
#To show all the commint on one line: we should use the following: git log --pretty=oneline
#To publish the change we should use: git push originm master
#To pull changes from the public repository to the local one we should use the following: git pull
#To create a new branch: we should use: git branch github2
#To switch to the other branch we use: git checkout github2
